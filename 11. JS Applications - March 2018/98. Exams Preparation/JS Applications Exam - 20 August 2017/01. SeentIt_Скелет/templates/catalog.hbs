{{> header}}
{{> navigation}}
<section id="viewCatalog">
    <div class="posts">
        {{#each posts}}
            {{> post}}
        {{else}}
            <span>No posts yet.</span>
        {{/each}}
    </div>
</section>
{{> footer}}